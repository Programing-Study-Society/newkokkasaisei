using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 日付 : MonoBehaviour
{   
    //スタート時に1回だけ実行
    void Start()
    {
        //コンソールにこんちはと表示
       Debug.Log("2");
    }

    // Update is called once per frame
    //舞フレーム実行
    void Update()
    {  //オブジェクトの位置の-x(左)座標に1だけ足し算
        //vector3.right=
        Debug.Log("3"); 
    }
}
