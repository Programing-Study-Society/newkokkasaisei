using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 日付1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(0);
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log(1);
    }
}
